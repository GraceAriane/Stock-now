package com.stocknow.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ──────────────────────────────────────────────────────────────────
val Teal400   = Color(0xFF00C896)   // primary accent (green-teal)
val Teal200   = Color(0xFF5DEAB5)   // lighter tint for selected states

// ── Neutrals (dark theme surfaces) ─────────────────────────────────────────
val Surface0  = Color(0xFF121212)   // deepest bg / status-bar / nav-bar
val Surface1  = Color(0xFF1A1A1A)   // screen background
val Surface2  = Color(0xFF232323)   // cards / list items
val Surface3  = Color(0xFF2E2E2E)   // elevated controls (stepper btn, thumb)

// ── Text ───────────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFE8E8E8)
val TextSecondary = Color(0xFF888888)
val TextHint      = Color(0xFF555555)

// ── Semantic ───────────────────────────────────────────────────────────────
val Red400    = Color(0xFFFF6B6B)   // sortie / danger
val Amber400  = Color(0xFFFFC44D)   // warning / alerte
val Blue400   = Color(0xFF5BA8FF)   // info

// ── Category icon bg tints ─────────────────────────────────────────────────
val IconBgGreen  = Color(0xFF1E2E26)
val IconBgPurple = Color(0xFF252030)
val IconBgAmber  = Color(0xFF2A2215)
val IconBgBlue   = Color(0xFF1E2530)
val IconBgRed    = Color(0xFF2A1515)

// ── Misc ───────────────────────────────────────────────────────────────────
val Divider = Color(0xFF2A2A2A)
